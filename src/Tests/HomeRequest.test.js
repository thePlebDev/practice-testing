import React from 'react';

import { shallow } from 'enzyme';
import HomeRequest from '../components/HomeRequest';

jest.moc('../Services/ticketMaster');

describe('should render',()=>{
  // const wrapper = shallow(<HomeRequest/>)
  // it('should render with the div element ',()=>{
  //   expect(wrapper.containsMatchingElement(<div></div>)).toEqual(true)
  // })
  // it('is should load an h1 with loading in it.',()=>{
  //   const wrapper = shallow(<HomeRequest/>)
  //   expect(wrapper.containsMatchingElement(<h1>Loading</h1>)).toEqual(true)
  // })

  it('fetches data on mount and renders it on mount',()=>{

  })

})
